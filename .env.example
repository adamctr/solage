# Database (PostgreSQL)
DB_HOST=postgres
DB_PORT=5432
DB_NAME=solage
DB_USER=solage
DB_PASSWORD=change-me

# Production only (docker-compose.prod.yml)
APP_DOMAIN=solage.example.com
ACME_EMAIL=ops@example.com
